var added_contact = $("<%= j(render "contacts/contact", contact: @contact, index: nil) %>");

added_contact.hide();

$("#contact_card_0").parent().before(added_contact);

added_contact.slideDown();

$("#new_contact_form").fadeOut(function() {
  $(this).remove();
});
