var added_group = $("<%= j(render "groups/group_all", group: @group) %>");

added_group.hide();

$("#group-table").prepend(added_group);

added_group.slideDown();

$("#new_group_form").fadeOut(function() {
  $(this).remove();
});