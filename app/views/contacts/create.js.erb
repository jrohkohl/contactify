var added_contact = $("<%= j(render "contacts/contact", contact: @contact, index: nil) %>");

added_contact.hide();

$("#contact-table").prepend(added_contact);

added_contact.slideDown();

$("#new_contact_form").fadeOut("slow", function() {
  $(this).remove();
});

