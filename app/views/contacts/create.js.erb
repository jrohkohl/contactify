var added_contact = $("<%= j(render "contacts/contact", contact: @contact) %>");

added_contact.hide();

$("#new_contact_form").after(added_contact);

added_contact.slideDown();

$("#new_contact_form").fadeOut(function() {
  $(this).remove();
});
