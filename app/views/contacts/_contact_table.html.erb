<div id = "contact-table" class = "row mt-3">

  <% contacts.by_created_date.each_with_index do |contact, index| %>

    <%= render "contacts/contact", contact: contact, index: index %>

  <% end %>

</div>
